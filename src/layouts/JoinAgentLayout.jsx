import React from 'react';
import { Outlet } from 'react-router-dom';

const JoinAgentLayout = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default JoinAgentLayout;